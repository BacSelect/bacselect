# Prospective Stage 2 repeated-BioSample execution

## Status

**PROSPECTIVE METHOD — NO REAL STAGE 2 OUTCOME GENERATED**

This document freezes the execution method for BacSelect selector-v1
Stage 2 repeated-BioSample reconciliation.

It is frozen after Stage 1 source-truth completion evidence was committed
and before any current Stage 2 repeated-BioSample fingerprint or decision
is generated.

No chromosome-integrity result, taxonomy-resolution result, complete
eligible-universe membership, external-holdout membership, structural
feature, OPS/SR distance, panel membership, coverage result, or selector
outcome is generated by this stage.

## Frozen repository parent

The Stage 2 method is written against BacSelect commit:

`0fc3dcf7b1a840303d39a92e4d8b4f4d81d35b03`

This commit contains the frozen Stage 1 completion evidence.

## Stage 1 input boundary

The completed Stage 1 decision table is:

`stage1-source-truth-decisions.tsv`

from the completed execution keyed by BacSelect commit:

`3abb710b7f03fb018c13628cc101bda5a4959512`

Its frozen SHA256 is:

`530ccec7679db9866ae04e81f05080094ace563a530a122c8ae97251801ea96d`

Stage 1 contains exactly:

- 68,480 decision rows;
- 68,359 `SUITABLE`;
- 121 `EXCLUDE_SOURCE_TRUTH`;
- 0 `REVIEW_UNRESOLVED`.

Only the 68,359 Stage 1 `SUITABLE` candidates enter Stage 2.

The 121 Stage 1 exclusions are terminal at the source-truth layer and do
not participate in BioSample grouping, fingerprint comparison, or
representative selection.

No Stage 1 excluded candidate can be reinstated or used to determine a
Stage 2 representative.

## Frozen Stage 1 completion evidence

The committed blinded Stage 1 evidence file is:

`validation/selector-v1/stage1-source-truth-completion-evidence.json`

SHA256:

`b459801d832137fb0399bc34dfe3611636ca1ffc0339c802a16a6216f9595dd2`

The Stage 2 execution must verify this file and the completed identity-bearing
Stage 1 decision-table SHA before reading Stage 1 membership.

## Frozen BioSample mapping

BioSample identity is obtained only from the already frozen final acquisition
manifests.

Historical-cache reuse manifest:

`cache-reuse-manifest.tsv`

SHA256:

`32a61975f99b973c3e7a2f58ac98beafe7b63c437c4bf0e3f7f51872680faff1`

It contains exactly 55,151 data rows.

Fresh-download manifest:

`fresh-download-manifest.tsv`

SHA256:

`1c9a73231d6b8ebfed76fb60621616588a4f51b1144e5d7880f14ddf26d1863b`

It contains exactly 15,326 data rows.

Together these manifests contain exactly the 70,477 frozen
metadata-retained accessions and must be disjoint by canonical versioned
GCA accession.

The authoritative mapping fields are:

- `canonical_genbank_assembly_accession`;
- `fresh_biosample`.

Every Stage 2 candidate must map to exactly one BioSample row.

Missing, duplicate, contradictory, malformed, or unmapped BioSample
evidence fails the execution closed.

The BioSample mapping must not be reconstructed from organism names,
taxonomy, historical manual adjudications, baseline membership, or any
selector-derived result.

## Frozen repeated-BioSample scientific rule

Stage 2 uses the already frozen composition implementation:

`src/bacselect/source_post_sequence_eligibility.py`

SHA256:

`62fa1e2f7d806f94b5f5eca73fb768745d3913a4b218a4d354562033cd300fe8`

and its frozen tests:

`tests/test_source_post_sequence_eligibility.py`

SHA256:

`14eeeb2837bfe07a3e5a6eb09c7c2bf1dec924302e114b2644852e1a5331a851`

The Stage 2 decision is delegated to:

`reconcile_repeated_biosamples()`

using `BioSampleMember` records.

No alternative repeated-BioSample decision implementation is permitted.

For one continuing BioSample member:

- status is `CONTINUE`;
- reason is `BIOSAMPLE_SINGLETON`.

For two or more continuing members with one identical assembly fingerprint:

- exactly the lexicographically smallest canonical versioned GCA accession
  is `CONTINUE`;
- its reason is `BIOSAMPLE_IDENTICAL_REPRESENTATIVE`;
- every other member is `NONREPRESENTATIVE`;
- their reason is `BIOSAMPLE_IDENTICAL_NONREPRESENTATIVE`.

For two or more continuing members with two or more distinct assembly
fingerprints:

- every member is `REVIEW_UNRESOLVED`;
- every member has reason `BIOSAMPLE_FINGERPRINTS_DIFFER`.

No historical manual repeated-BioSample adjudication is consulted.

The previously observed repeated-BioSample group count is not an expected
Stage 2 outcome. Grouping is recomputed only after Stage 1 source-truth
filtering.

## Frozen assembly fingerprint

The topology-aware assembly fingerprint implementation is:

`src/bacselect/source_fingerprint.py`

SHA256:

`6c994d243709abdbe9d7c8949e156009b9f31f3fcef3247cc3c5679e2fff41c9`

The schema is:

`project-finch-topology-aware-sequence-v1`

The inherited Project Finch algorithmic reference is:

- Project Finch commit:
  `5edcc8168c9d521f3e065489bd36bc9db94e1406`
- path:
  `scripts/experiment-0/build_repeated_biosample_fingerprints.py`
- SHA256:
  `0d7ced2afde95141d42efbf5d249a91315ed44a3a0646a0c35fd1dee177c4d6a`

Only the frozen algorithmic semantics are inherited. Historical Project
Finch repeated-BioSample memberships or adjudications are not inherited.

For each verified Primary Assembly component, Stage 2 uses the frozen
topology-aware component sequence identity.

For each assembly, Stage 2 uses the frozen topology-aware assembly
fingerprint over all retained Primary Assembly components.

Stage 1 `sequence_set_sha256` is not a substitute for this fingerprint.

## Frozen source-evidence reconstruction

Stage 2 reuses the already frozen Stage 1 evidence-reconstruction
implementation:

`src/bacselect/source_truth_execution.py`

SHA256:

`83b8ec7fce774c0b68cb2af982aef13904c6b64b3ee695512c578f98e5de9b92`

and frozen tests:

`tests/test_source_truth_execution.py`

SHA256:

`314b417ee05e2293ad37268963d9bdddb5bc58c9d6d6bc102a6cebe6ec926864`

The execution must reuse the frozen:

- candidate-audit loading semantics;
- component-audit loading semantics;
- package-manifest loading semantics;
- `_match_candidate_package_row()` binding semantics;
- `load_primary_components()` reconstruction semantics;
- `source_evidence_sha256()` semantics.

No independent FASTA/package resolver may be introduced for Stage 2.

In particular, the Recovery 001 package binding remains authoritative:

1. an exact candidate package path takes precedence;
2. if an exact path exists, its SHA must equal the candidate SHA or the
   execution fails immediately;
3. if no exact path exists, matching is by exact FASTA basename plus exact
   candidate SHA256;
4. exactly one such package row is required;
5. zero or multiple matches fail closed;
6. no accession-prefix, fuzzy-path, or other heuristic matching is allowed.

The reconstructed FASTA must again pass the frozen file-size, file-SHA,
component-count, component-length, component-SHA, and topology evidence
checks before it can contribute to a Stage 2 fingerprint.

## Stage 1 evidence-binding invariant

For every one of the 68,359 Stage 2 candidates, Stage 2 must recompute the
frozen `source_evidence_sha256()` from the candidate audit, component
evidence, and package manifest.

The recomputed value must equal that candidate's
`source_evidence_sha256` recorded in the frozen Stage 1 decision table.

Any mismatch fails the entire Stage 2 execution closed before a Stage 2
decision artifact is finalized.

This requirement ensures that Stage 2 fingerprints exactly the source
evidence that Stage 1 classified.

## Production population reconstruction

Production execution reconstructs the same historical and fresh
sequence-evidence population used by the completed Stage 1 execution.

The Stage 1 decision table determines which candidates may continue.

The production Stage 2 candidate set is therefore exactly:

`Stage 1 source_truth_status == SUITABLE`

and must contain exactly 68,359 unique canonical versioned GCA accessions.

No baseline membership, historical-cache membership as a scientific
criterion, species identity, organism identity, TaxID, chromosome outcome,
structural feature, or selector outcome may alter this population.

## Execution order

The production Stage 2 wrapper must perform these operations in order:

1. verify repository commit and clean working tree;
2. verify every frozen repository-file identity required by this method;
3. verify the frozen Stage 1 completion evidence;
4. verify the exact Stage 1 decision-table SHA and cardinality;
5. reconstruct the 68,359 unique Stage 1 `SUITABLE` membership;
6. verify the two frozen acquisition manifests and their exact row counts;
7. verify their accession sets are disjoint and total 70,477;
8. map every Stage 2 accession to exactly one frozen `fresh_biosample`;
9. reconstruct the same frozen candidate/component/package evidence used
   by Stage 1;
10. freeze predecision provenance and Stage 2 input membership count plus
    cryptographic membership fingerprint;
11. for each Stage 2 candidate, reconstruct and verify its Primary Assembly
    components using the frozen Stage 1 loader;
12. recompute and verify its Stage 1 `source_evidence_sha256`;
13. compute its frozen topology-aware assembly fingerprint;
14. construct `BioSampleMember` records;
15. delegate all repeated-BioSample decisions to the frozen
    `reconcile_repeated_biosamples()` implementation;
16. verify exactly one Stage 2 decision exists for every one of the 68,359
    input candidates;
17. write deterministic identity-bearing scratch outputs;
18. write blinded aggregate counts and cryptographic fingerprints;
19. atomically finalize the production execution directory.

No Stage 2 outcome may be calculated before the predecision provenance
artifact is written.

## Determinism

All identity-bearing rows are sorted deterministically before serialization.

Canonical versioned GCA accession is the candidate identity.

BioSample groups are derived exclusively from the frozen BioSample mapping.

Representative choice is performed exclusively by the frozen reconciliation
primitive.

No filesystem enumeration order, dictionary insertion order, acquisition
route, batch number, source organism, taxonomy, baseline membership, or
historical adjudication may affect the scientific result.

## Fail-closed conditions

The entire Stage 2 execution fails closed on any of the following:

- repository identity mismatch;
- dirty repository;
- frozen-file SHA mismatch;
- Stage 1 decision-table SHA mismatch;
- Stage 1 decision count other than 68,480;
- Stage 1 `SUITABLE` count other than 68,359;
- duplicate Stage 1 accession;
- unexpected Stage 1 status;
- acquisition-manifest SHA mismatch;
- acquisition-manifest row-count mismatch;
- overlap between cache-reuse and fresh-download accession sets;
- combined acquisition-manifest count other than 70,477;
- missing or duplicate BioSample mapping;
- malformed accession or BioSample evidence;
- candidate/component/package evidence mismatch;
- Stage 1 source-evidence SHA mismatch;
- invalid topology-aware fingerprint;
- duplicate Stage 2 decision;
- missing Stage 2 decision;
- unknown frozen primitive status or reason;
- any impossible accounting combination.

No failed production directory is deleted, overwritten, renamed as a
successful result, or reused.

## Identity-bearing outputs

All accession-bearing, BioSample-bearing, and assembly-fingerprint-bearing
Stage 2 outputs remain outside Git in scratch storage.

The production execution writes deterministic identity-bearing artifacts
including:

- Stage 2 candidate decisions;
- Stage 2 BioSample-group evidence;
- Stage 2 input-evidence manifest;
- Stage 2 predecision provenance;
- Stage 2 execution provenance;
- Stage 2 content manifest.

The exact production wrapper may refine filenames prospectively before
production execution, but may not change the scientific semantics frozen
here.

## Blinded aggregate output

Git may subsequently freeze only non-identity Stage 2 evidence, including:

- Stage 2 input candidate count;
- Stage 2 input membership SHA256;
- BioSample-group count;
- singleton-group count;
- repeated-group count;
- identical repeated-group count;
- differing repeated-group count;
- `CONTINUE` count;
- `NONREPRESENTATIVE` count;
- `REVIEW_UNRESOLVED` count;
- deterministic reason counts;
- cryptographic identities of the identity-bearing scratch artifacts;
- execution provenance identities.

The following accounting identities must hold:

`CONTINUE + NONREPRESENTATIVE + REVIEW_UNRESOLVED = 68,359`

and:

`singleton groups + repeated groups = all Stage 2 BioSample groups`

and:

`identical repeated groups + differing repeated groups = repeated groups`

No expected repeated-group, identical-group, differing-group,
non-representative, or unresolved count is frozen prospectively.

## Downstream boundary

Stage 2 ends after repeated-BioSample reconciliation.

It must not generate or inspect:

- chromosome-integrity outcomes;
- taxonomy outcomes;
- complete eligible-universe membership;
- external-holdout membership;
- structural-feature values;
- OPS ladder membership;
- SR ladder membership;
- OPS/SR distances;
- panel identities;
- panel coverage;
- selector outcomes.

Only candidates with Stage 2 status `CONTINUE` may proceed to Stage 3
chromosome-component integrity.

A later chromosome-integrity failure must not promote a Stage 2
non-representative BioSample member as a replacement representative.

## Blinding

Identity-bearing Stage 2 outputs remain scratch-only.

Before the selector-resolution decision checkpoint, human inspection is
limited to aggregate counts, cryptographic fingerprints, execution
provenance, and validation status.

Candidate identities, BioSample identities, assembly fingerprints,
representative identities, unresolved-group identities, and downstream
selector information are not inspected.

## Prospectivity statement

At the time this method is frozen:

- Stage 1 source-truth execution is complete and frozen;
- the Stage 2 scientific primitives are already frozen;
- the Stage 2 production population is fixed by the frozen Stage 1 result;
- the BioSample source is fixed by frozen acquisition manifests;
- no current Stage 2 fingerprint has been generated;
- no current repeated-BioSample outcome has been generated;
- no chromosome-integrity outcome has been generated;
- no taxonomy-resolution outcome has been generated;
- no complete eligible universe has been generated;
- no final external holdout has been generated;
- no structural feature has been calculated for that holdout;
- no OPS/SR selector outcome has been calculated.

Implementation and synthetic testing are frozen in Git before any real
Stage 2 production execution.
